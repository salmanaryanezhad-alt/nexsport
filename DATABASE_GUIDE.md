# راهنمای اتصال دیتابیس MySQL و PostgreSQL در NexSport

سامانه NexSport با یک معماری چندگانه و ماژولار طراحی شده است و به صورت خودکار از هر دو پایگاه‌داده محبوب **MySQL / MariaDB** (ویژه هاست‌های اشتراکی سی‌پنل و دایرکت‌ادمین) و **PostgreSQL** (ویژه ورسل و سرورهای ابری) پشتیبانی می‌کند.

در صورتی که هیچ دیتابیسی تنظیم نشود، برنامه به صورت خودکار روی حافظه موقت (In-Memory) کار می‌کند تا برنامه هیچ‌وقت به خاطر عدم تنظیم دیتابیس کرش نکند.

---

## ۱. نحوه راه‌اندازی و اتصال پایگاه‌داده MySQL (هاست سی‌پنل / دایرکت‌ادمین)

این روش برای کاربرانی است که برنامه را روی هاست لینوکس با Node.js Selector اجرا می‌کنند.

### الف) اطلاعات آماده اتصال
اطلاعات دیتابیس شما در فایل `.env` یا بخش Environment Variables هاست به صورت زیر تعریف می‌شود:

```env
DB_TYPE=mysql
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=unmvwyxf_nexsport_db
MYSQL_PASSWORD=HKV0=VNVFXzB*Yxf
MYSQL_DATABASE=unmvwyxf_nexsport_db
```

### ب) ایجاد خودکار جداول
هنگام اتصال برنامه، تمام جدول‌های مورد نیاز زیر به صورت کاملاً خودکار با انکودینگ فارسی `utf8mb4_unicode_ci` ساخته می‌شوند:
1. `users` (کاربران، مدیران، مربیان)
2. `email_verifications` (کدهای تایید ۶ رقمی)
3. `sessions` (نشست‌های ورود لغزان ۴۸ ساعته)
4. `password_resets` (کدهای بازیابی رمز عبور)
5. `tournaments` (ذخیره ابری مسابقات و جدول‌ها)

> **اختیاری (ایمپورت دستی):** اگر تمایل دارید قبل از اجرای برنامه، جدول‌ها را به صورت دستی بسازید، کافی است فایل `lib/db/schema_mysql.sql` را باز کرده و در بخش **SQL** یا **Import** در phpMyAdmin اجرا کنید.

---

## ۲. نحوه اتصال پایگاه‌داده PostgreSQL (سرور ابری / Vercel)

در صورتی که برنامه را روی Vercel یا سرورهای ابری مثل Neon مستقر می‌کنید:
1. متغیر `DATABASE_URL` را با مقدار رشته اتصال پستگرس تعریف کنید:
   ```env
   DATABASE_URL=postgres://user:password@host:5432/dbname?sslmode=require
   ```
2. سیستم به طور خودکار درایور PostgreSQL را شناسایی و جداول مربوطه را فعال می‌کند.

---

## ۳. فعال‌سازی ارسال واقعی ایمیل تاییدیه (سرویس Resend - اختیاری)

1. در سایت [resend.com](https://resend.com) یک حساب رایگان بسازید.
2. کلید API را دریافت کنید.
3. متغیرهای زیر را در فایل `.env` قرار دهید:
   * `RESEND_API_KEY`: کلید دریافتی از Resend
   * `EMAIL_FROM`: نام و ایمیل ارسال‌کننده (مثلاً: `NexSport <noreply@nexsport.ir>`)

> **توجه:** در صورت عدم تعریف متغیرهای ایمیل، سامانه در حالت تست امنیتی قرار گرفته و کد ۶ رقمی را مستقیماً در پاسخ به کاربر نمایش می‌دهد تا ثبت‌نام متوقف نشود.
