# راهنمای اتصال دیتابیس PostgreSQL و ایمیل در NexSport (فاز دوم)

سامانه نکس‌پورت به گونه‌ای طراحی شده است که در حال حاضر حتی بدون اتصال دیتابیس نیز به صورت پایدار و بدون قطعی (با استور آزمایشی) روی ورسل کار می‌کند. برای فعال‌سازی دیتابیس دائمی و ارسال واقعی ایمیل، مراحل زیر را دنبال فرمایید:

---

## ۱. نحوه اتصال پایگاه‌داده PostgreSQL در ورسل (Vercel)

شما می‌توانید از هر سرویس‌دهنده رایگان یا تجاری PostgreSQL استفاده کنید:

### روش اول: فعال‌سازی با یک کلیک در پنل ورسل (Vercel Storage)
1. وارد داشبورد پروژه خود در سایت [vercel.com](https://vercel.com) شوید.
2. به تب **Storage** بروید و روی **Create Database** کلیک کنید.
3. گزینه **Postgres (Neon)** را انتخاب کنید.
4. دیتابیس را به پروژه `nexsport` متصل (Connect) کنید.
5. ورسل متغیرهای `POSTGRES_URL` و `DATABASE_URL` را به صورت خودکار به پروژه اضافه می‌کند.
6. با اولین درخواست، تمام جداول (`users`, `email_verifications`, `sessions`) به طور خودکار ساخته خواهند شد!

### روش دوم: استفاده از دیتابیس‌های ابری رایگان دیگر (Neon.tech یا Supabase)
1. یک پروژه رایگان در [Neon.tech](https://neon.tech) یا [Supabase.com](https://supabase.com) بسازید.
2. رشته اتصال (Connection String) را کپی کنید (فرمت: `postgres://user:pass@host/dbname?sslmode=require`).
3. در داشبورد ورسل، به مسیر **Settings > Environment Variables** بروید.
4. متغیر جدیدی با نام `DATABASE_URL` ایجاد کرده و رشته اتصال را در مقدار آن قرار دهید.

---

## ۲. فعال‌سازی ارسال واقعی ایمیل تاییدیه (اختیاری)

سامانه نکس‌پورت به صورت پیش‌فرض از سرویس مدرن **Resend** (با ۳۰۰۰ ایمیل رایگان در ماه) پشتیبانی می‌کند:

1. در سایت [resend.com](https://resend.com) یک حساب رایگان بسازید.
2. یک API Key جدید بسازید.
3. در داشبورد ورسل در بخش **Environment Variables** دو متغیر زیر را تعریف کنید:
   * `RESEND_API_KEY`: کلید دریافتی از Resend (مثال: `re_123456789...`)
   * `EMAIL_FROM`: ایمیل ارسال‌کننده (مثال: `NexSport <onboarding@resend.dev>` یا دامنه اختصاصی خودتان)

> **توجه:** در صورت عدم تعریف متغیرهای ایمیل، سامانه در حالت تست خودکار اجرا شده و کد ۶ رقمی را مستقیماً در صفحه به شما نشان می‌دهد تا فرآیند ثبت‌نام بدون وقفه انجام شود.
