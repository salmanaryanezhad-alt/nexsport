# Apache / LiteSpeed rewrite for NexSport PHP API
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /api/
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteRule ^(.*)$ index.php?_route=$1 [QSA,L]
</IfModule>
